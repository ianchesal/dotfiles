#compdef claude
# Generated by script/gen-claude-completions.py from claude 2.1.274 (Claude Code)
# Do not edit by hand — regenerate with: just claude::gen-completions

_claude() {
  local context state state_descr line
  typeset -A opt_args

  _arguments -C \
    '--add-dir[Additional directories to allow tool]:directories:' \
    '--agent[Agent for the current session.]:agent:' \
    '--agents[JSON object defining custom agents (e.g.]:json:' \
    '--allow-dangerously-skip-permissions[Enable bypassing all permission checks]' \
    '--append-system-prompt[Append a system prompt to the default]:prompt:' \
    '--autocompact[Auto-compact window size (auto, or]:auto|tokens:' \
    '--ax-screen-reader[Render screen-reader friendly output]' \
    '--bg[Start the session in the background and]' \
    '--background[Start the session in the background and]' \
    '--bare[Minimal mode: skip hooks, LSP, plugin]' \
    '--betas[Beta headers to include in API requests]:betas:' \
    '--brief[Enable SendUserMessage tool for]' \
    '--chrome[Enable Claude in Chrome integration]' \
    '--cloud[Create a cloud session with the given]::description|session_id|url:' \
    '(-c --continue)'{-c,--continue}'[Continue the most recent conversation in]' \
    '--dangerously-skip-permissions[Bypass all permission checks.]' \
    '(-d --debug)'{-d,--debug}'[Enable debug mode with optional category]::filter:' \
    '--debug-file[Write debug logs to a specific file path]:path:' \
    '--disable-slash-commands[Disable all skills]' \
    '--effort[Effort level for the current session]:level:' \
    '--environment[Create a new cloud session that runs on]:environment_id:' \
    '--fallback-model[Enable automatic fallback to specified]:model:' \
    '--file[File resources to download at startup.]:specs:' \
    '--fork-session[When resuming, create a new session ID]' \
    '--forward-subagent-text[Forward subagent text and thinking]' \
    '--from-pr[Resume a session linked to a PR by PR]::value:' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '--ide[Automatically connect to IDE on startup]' \
    '--include-hook-events[Include all hook lifecycle events in the]' \
    '--include-partial-messages[Include partial message chunks as they]' \
    '--input-format[Input format (only works with --print):]:format:' \
    '--json-schema[JSON Schema for structured output]:schema:' \
    '--max-budget-usd[Maximum dollar amount to spend on API]:amount:' \
    '--mcp-config[Load MCP servers from JSON files or]:configs:' \
    '--model[Model for the current session.]:model:' \
    '(-n --name)'{-n,--name}'[Set a display name for this session]:name:' \
    '--no-chrome[Disable Claude in Chrome integration]' \
    '--no-session-persistence[Disable session persistence - sessions]' \
    '--output-format[Output format (only works with --print):]:format:' \
    '--permission-mode[Permission mode to use for the session]:mode:' \
    '--permission-prompts[Who answers permission prompts with]:target:' \
    '--plugin-dir[Load a plugin from a directory or .zip]:path:' \
    '--plugin-url[Fetch a plugin .zip from a URL for this]:url:' \
    '(-p --print)'{-p,--print}'[Print response and exit (useful for]' \
    '--prompt-suggestions[Enable prompt suggestions.]::value:' \
    '--remote-control[Start an interactive session with Remote]::name:' \
    '--replay-user-messages[Re-emit user messages from stdin back on]' \
    '--restricted[Restricted mode: removes the built-in]' \
    '(-r --resume)'{-r,--resume}'[Resume a conversation by session ID, or]::value:' \
    '--safe-mode[Start with all customizations]' \
    '--session-id[Use a specific session ID for the]:uuid:' \
    '--setting-sources[Comma-separated list of setting sources]:sources:' \
    '--settings[Path to a settings JSON file or a JSON]:file-or-json:' \
    '--strict-mcp-config[Only use MCP servers from --mcp-config,]' \
    '--system-prompt[System prompt to use for the session]:prompt:' \
    '--system-prompt-snapshot[Record the system prompt once per]:on|off:' \
    '--teleport[Resume a teleport session, optionally]::session:' \
    '--tmux[Create a tmux session for the worktree]' \
    '--tools[Specify the list of available tools from]:tools:' \
    '--verbose[Override verbose mode setting from]' \
    '(-v --version)'{-v,--version}'[Output the version number]' \
    '(-w --worktree)'{-w,--worktree}'[Create a new git worktree for this]::name:' \
    '1:prompt:' \
    '*::args:->subcmd' && return 0

  case $state in
    subcmd)
      local -a subcommands
      subcommands=(
      'auth:Manage authentication'
      'auto-mode:Inspect or reset auto mode classifier'
      'doctor:Check the health of your Claude Code'
      'mcp:Configure and manage MCP servers'
      'plugin:Manage Claude Code plugins'
      'project:Manage Claude Code project state'
      'setup-token:Set up a long-lived authentication token'
      'update:Check for updates and install if'
      )
      _describe 'subcommand' subcommands
      ;;
  esac
}

_claude "$@"
