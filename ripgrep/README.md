# dotfiles/ripgrep

Configuration for [ripgrep](https://github.com/BurntSushi/ripgrep), a fast recursive search tool. `ripgrep.conf` sets max column width with preview, smart-case matching, line-number color styling, and a glob to exclude `rendered/*`.

## Tasks

- `rake ripgrep` — Install ripgrep dotfiles
