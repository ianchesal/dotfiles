# dotfiles/viddy

Configuration for [viddy](https://github.com/sachaos/viddy), a modern `watch` replacement. `viddy.toml` runs commands through zsh and rebinds the time-machine navigation keys.

## Tasks

- `rake viddy` — Install all viddy dotfiles
