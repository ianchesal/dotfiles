# My aws customizations

# If we have the Homebrew-installed awscli package, source completions
if [[ -a /usr/local/share/zsh/site-functions/_aws ]]; do
  source /usr/local/share/zsh/site-functions/_aws
done
