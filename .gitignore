# macOS
.DS_Store

# Ruby
/.bundle/
/Gemfile.lock

# Python
__pycache__/
*.pyc

# Node.js
/node_modules/
/package.json
/package-lock.json

# Claude Code - project .claude directory
# Ignore everything except explicitly shared config
/.mcp.json
/.claude/*
!/.claude/skills/
!/.claude/settings.json

# Work-only Claude settings -- machine-local, never published (this repo is
# public). See the Claude Configuration section of CLAUDE.md.
/home/dot_claude/oracle-gateway.json

# Git
# NOTE: git/ignore IS tracked -- it's the shared global gitignore that git picks
# up via its XDG default at ~/.config/git/ignore. Machine-specific git settings
# (credential helpers, maintenance.repo paths) belong in git/local instead.

# Homebrew
/brew/Brewfile.lock.json

# Superpowers scratch review output -- never committed. docs/superpowers/ IS
# committed: prompts handed to another agent have to reach the PR, and pax reads
# them from the planning branch.
/SPEC_REVIEW.md

# Serena
/.serena/

# Pi
/.pi/

# Misc
BROWSE
